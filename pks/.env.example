# ============================================
# PKS 开发环境配置示例
# ============================================
# 复制此文件为 .env 并根据你的环境修改配置
# cp .env.example .env

# ============================================
# 前端配置
# ============================================
# 前端访问的后端 API 地址
# - 本地访问使用: http://localhost:8000/api/v1
# - 虚拟机/远程访问使用: http://192.168.0.16:8000/api/v1 (替换为你的实际 IP)
API_URL=http://localhost:8000/api/v1

# ============================================
# 后端配置 (可选，通常使用默认值即可)
# ============================================
# 应用名称
# APP_NAME=PKS

# 应用版本
# APP_VERSION=1.0.0

# 调试模式
# DEBUG=True

# JWT 密钥 (生产环境请修改)
# SECRET_KEY=dev-secret-key-change-in-production

# 数据库 URL (默认使用 SQLite)
# DATABASE_URL=sqlite:///./data/pks.db

# CORS 允许的源 (JSON 数组格式)
# BACKEND_CORS_ORIGINS=["http://localhost","http://localhost:5173","http://frontend:80"]

# JWT Token 过期时间 (分钟)
# ACCESS_TOKEN_EXPIRE_MINUTES=60

# JWT Refresh Token 过期时间 (天)
# REFRESH_TOKEN_EXPIRE_DAYS=7
