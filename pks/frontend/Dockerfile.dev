# ============================================
# PKS Frontend 开发环境 Dockerfile
# 基础镜像: node:18-alpine
# ============================================

FROM node:18-alpine

WORKDIR /app

# 安装依赖
COPY package*.json ./
RUN npm config set registry https://registry.npmmirror.com && \
    npm install

# 暴露 Vite 开发服务器端口
EXPOSE 5173

# 启动开发服务器（热重载）
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
